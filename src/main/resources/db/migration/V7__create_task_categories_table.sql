CREATE TABLE IF NOT EXISTS task_categories (
                                               id BIGSERIAL PRIMARY KEY,
                                               type VARCHAR(50),
    name VARCHAR(255),
    color VARCHAR(50)
    );
